# Design tokens

The initial web shell keeps its tokens in `apps/web/app/globals.css`. This package is reserved for extracting the stable semantic token set after the first UX review, before multiple domain surfaces are built.
